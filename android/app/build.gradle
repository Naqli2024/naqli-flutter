plugins {
    id "com.android.application"
    id "kotlin-android"
    id "dev.flutter.flutter-gradle-plugin"
//    id 'com.google.gms.google-services'
}

android {
    namespace = "com.example.flutter_naqli"
    compileSdk = 36
    ndkVersion = "28.0.12433566"

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }

    kotlinOptions {
        jvmTarget = "17"
    }

    defaultConfig {
        applicationId = "com.app.naqlee"
        minSdkVersion = flutter.minSdkVersion
        targetSdk = 36
        versionCode = 26
        versionName = "1.0.9"
        buildConfigField "String", "API_KEY", "\"${project.hasProperty('API_KEY') ? project.properties['API_KEY'] : ''}\""

        ndk {
            abiFilters "armeabi-v7a", "arm64-v8a"
        }
    }

//    splits {
//        abi {
//            enable true
//            reset()
//            include 'armeabi-v7a', 'arm64-v8a'
//            universalApk false
//        }
//    }

    buildFeatures {
        buildConfig true
    }

    signingConfigs {
        release {
            storeFile file("D:/naqli-flutter/android/app/upload-keystore.jks")
            storePassword "naqli@123"
            keyAlias "upload"
            keyPassword "naqli@123"
        }
    }

    buildTypes {
        release {
            signingConfig signingConfigs.release
            minifyEnabled true
            shrinkResources true
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
        debug {
            buildConfigField "String", "API_KEY", "\"${project.hasProperty('API_KEY') ? project.properties['API_KEY'] : ''}\""
        }
    }

    packagingOptions {
        jniLibs {
            useLegacyPackaging false
        }
    }
}
dependencies {
    implementation "com.google.android.gms:play-services-maps:19.0.0"
    implementation 'com.stripe:stripe-android:20.48.6'

    implementation 'androidx.core:core-ktx:1.13.1' // Latest for KTX
    implementation 'androidx.appcompat:appcompat:1.7.0' // Latest stable appcompat
    implementation 'androidx.fragment:fragment-ktx:1.7.1' // Latest fragment-ktx
    implementation 'androidx.lifecycle:lifecycle-runtime-ktx:2.8.0' // Latest lifecycle-ktx
}

flutter {
    source = "../.."
}

//apply plugin: 'com.google.gms.google-services'
